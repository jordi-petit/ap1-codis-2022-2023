from yogi import *

pics = 0
a = scan(int)
if a is not None:
    b = scan(int)
    if b is not None:
        for c in tokens(int)
           if a < b > c:
                pics = pics + 1
            a, b = b, c
print(pics)
